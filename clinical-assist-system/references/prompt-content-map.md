# 首诊 Prompt 内容地图与"割肉"史（2026-07-21 只读侦察，main @ 92ddb64）

> 用途：①回切本地/换快服务器后决定"哪些被精简的内容值得恢复"；②改 prompt 前核对现状。行号基于 92ddb64，改动后需重新定位。

## 1. 后端路由与端点现状

- `/clinical/assist`（L2359）→ L2591 `_call_clinical_llm`（L2099-2128）按 `RAG_LLM_PROVIDER`（L48，默认 `online`）路由：DeepSeek→GLM→本地 vLLM。三种 mode 共用同一份 user prompt，只差 system prompt 与 max_tokens：quick=2600 / detail=1800 / full=2500（L2579-2584）。
- ⚠️ **`/compliance/{visit_id}?mode=full`（L836-918）绕过路由**：L884-898 直连 DeepSeek（URL/model 硬编码，key 只认 `DEEPSEEK_API_KEY`，timeout=30，max_tokens=1024），失败才回退本地。改 `RAG_LLM_PROVIDER=local` 时此端点仍打 DeepSeek。
- `SPARK_API_URL/SPARK_API_KEY/SPARK_MODEL`（L2052-2054）是死配置，全文件无调用点。

## 2. 当前 user prompt 段落地图（L2424-2576，三种 mode 共用）

| 段落 | 行号 | 截断规格 |
|:--|:--|:--|
| [医生已选择手术操作/诊断]（条件注入） | 2425-2440 | 不截断 |
| [病情描述和治疗想法] | 2445 | 不截断（医师原文） |
| [患者基本信息] | 2447-2470 | 主诉(EMR)[:200] / 现病史[:300] / 既往史[:200] / 体格检查[:200] / 过敏史[:100] |
| [现有诊断] | 2472-2476 | 全量 |
| [异常检验结果] | 2478-2480 | ≤8条，每条[:120]（源头只保留异常项） |
| [影像/内镜报告] | 2482-2484 | ≤5条，每条[:150]（源头 imaging_summary 每条诊断[:100]，L1795） |
| [已开医嘱] | 2486-2488 | ≤8条，item[:60] |
| [已做手术记录] | 2490-2496 | 全量（名称/编码/状态/时间） |
| [病历首页] | 2498-2510 | 入/出院诊断不截断；诊疗经过[:300] / 出院医嘱[:200] |
| [费用] | 2512-2514 | 全量 |
| [淮南DIP分值库参考] | 2516-2521 | top6 按 relevance（源头 content[:400]） |
| [淮南市医疗服务价格参考] | 2524-2534 | ≤4条（源头[:300]） |
| [和华2025 DIP结算参考] | 2537-2541 | ≤3条（源头[:400]） |
| [相似病例参考] | 2543-2548 | ≤3条，每条[:200]（源头[:300]） |
| [合规规则参考] | 2550-2555 | ≤3条，每条[:200]（源头[:300]） |
| [首诊结论]（仅 detail 注入） | 2557-2574 | 次诊断≤3、主操作≤2 |

## 3. "割肉"清单（为本地 10tps 速度所做）

**病历段落从未被删段或改截断长度**（git init 8e3b6d4 与 HEAD 完全一致）——割的全在 system prompt 与输出侧：

| 内容 | 提交 | 说明 |
|:--|:--|:--|
| 指令 16 条 → 9 条 | `1bd4faf` | 删：用药/耗材/收费三类合规分析细则（超适应症、配伍禁忌、分解收费、自立项目等）、"控费建议=分值×0.65299对比预估总费用"、"用药需标注仅供参考"、"医保合规特别标注"、"结算参考数据用于控费指导"等 |
| compliance_analysis 模板 verbose→单行 | `1bd4faf` | 三个子对象各 3 字段 → "药占比简述/耗占比简述/控费简述" |
| 病程 100-200字 → 30-60字 | `1bd4faf`（现 L2223） | |
| quick 不输出 医嘱/合规/病程/DIP分值 | `1bd4faf` | system prompt ~2000→~800 token；被砍内容移至 detail |
| quick 回填首次病程+鉴别丰富化 | `071c5bc` | 部分质量回补，quick max_tokens 1600→2600 |
| max_tokens 轨迹 | `3afbeeb`(1200/500) → `1ba9308`(4096/2048) → `bc68dfd`(2000/1024) → `1bd4faf`(分模式) | ⚠️ `bc68dfd` 提交消息写"1200→2000"，实际 diff 是 4096→2000——**审计以 diff 为准，勿信提交消息** |
| pre-git（无 commit） | SKILL.md session 5 记录 | 指令 15→8 条、compliance 嵌套精简、130s→99s；仓内无更早历史可查 |

## 4. 恢复建议（换快服务器后）

本地 vLLM 提速后（或回切 local 时）优先考虑：恢复三类合规分析细则指令、病程字数放宽回 100-200 字、detail 模式 max_tokens 1800→2500+。病历段落截断规格本身不是速度牺牲品，不必动。
